function _abs(number){
    // js Math 对象方法 abs（）求绝对值
    return Math.abs(number)
    // 三元运算符
    return number >= 0 ? number:-number;
    // if 条件判断
    if (number>0)return number;
    else (return - number)
}
